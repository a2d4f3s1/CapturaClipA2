# 変更履歴

## 未リリース

### 追加

- **クリップボードの画像を、置き所を決めてつなげるようになりました。** 右クリックメニューの「画像」→「クリップボードの画像を連結する...」です。**上下左右のどこにつなぐか、間にどれだけ隙間を空けるか、そこからどれだけずらすか**を決められます。ずらすと 2 枚を重ねることもでき、そのときどちらを上にするかも選べます。**値を動かすとウィンドウの絵がその場で変わる**ので、決める前に結果そのものを見られます。**今開いている絵は画面の上で動きません** ―― 上や左につないでも、伸びるのは足す絵の側だけに見えます

- **重なって置いたものを、続けて押すだけで下から選べるようになりました。** 同じ所を押すたびに 1 つ下のものへ移り、いちばん下まで行くと先頭へ戻ります。上のものをどけなくても、その下にあるものに手が届きます

- **置いたものを、クリックひとつで選択に足したり外したりできるようになりました。** `Shift` を押しながら押せば足し、`Alt` なら外します。囲む操作は今までどおりです

- **選ぶ 4 つのツールが、カーソルで見分けられるようになりました。** 矢印の右下に付く印が、四角なら矩形で囲む、輪なら手で囲む、中が空なら範囲を選ぶ、中が詰まっていれば置いたものを選ぶことを表します。色は範囲が青、置いたものが緑で、`Alt` を押している間はどれも赤（＝外す）になります

- **設定の「全般」に「掴める範囲」と「連結の隙間」が増えました。** 掴める範囲を広げると、選んだものに付く帯も一緒に太くなります ―― **見えている所がそのまま掴める範囲**です

- **フォントを名前で絞り込んで選べるようになりました。** 右クリックメニューの「文字」→「フォント...」（入力中の右クリックからも同じ）で小さな窓が開き、**名前の一部を打つと一覧が絞られます**。`Enter` か一覧のダブルクリックで決まり、`Esc` か外を押すと何も変えずに閉じます。打ちながら `↑` `↓` で一覧を歩けます。**大文字小文字・全角半角・ひらがなとカタカナの違いは無視される**ので、`ms` で「ＭＳ ゴシック」、`ごしっく`（変換前のままで可）で「ゴシック」系が出ます。**英語名でも引けます** ―― `yu` で「游ゴシック」、`meiryo` で「メイリオ」が出るので、日本語名のフォントを探すのに日本語入力へ切り替える必要がありません

### 変わったこと

- **フォントの一覧が、画面いっぱいに広がらなくなりました。** これまでは 365 件が 30 個ずつ 13 列に折り返して画面幅を埋めており、目で探すしかありませんでした。1 枚の小さな窓に変わり、絞り込みで目当ての名前に届きます

### 直したこと

- **「表示」→「500%」が、拡大せずにフォントを変えてしまっていた不具合を直しました。** メニュー項目の内部番号が、フォント一覧の先頭と重なっていたためです

- **2 つのフォントにまたがる範囲を選んだとき、片方のフォントに印が付いてしまう**のをやめました。範囲の中でフォントが揃っていないときは、どれにも印を付けません

- **`Ctrl + W` / `Ctrl + L` は、どこからでもオブジェクト選択に入るようになりました。** これまでは同じキーをもう一度押すと元のツールに戻っていましたが、入ったつもりが出てしまうことがあったため、他のツールキーと同じ「押したらそこへ行く」に揃えました。抜けるときは他のツールのキーを押してください

- **どのオブジェクトを選んでいるかも、取り消しで 1 つずつ戻せるようになりました。** 選び直しに行き過ぎても、`Ctrl + Z` 一回で直前の選択に戻れます。これまでは選択が取り消しの対象ではなかったため、`Ctrl + Z` が直前に描いた線まで戻ってしまっていました

- **`Shift` や `Alt` を押している間は、ものの上でもカーソルが十字矢印になりません。** そのとき押しても掴めない（足す・外すになる）ので、掴めるという表示をやめました

- **設定で、範囲選択とオブジェクト選択のキーが対で決まるようになりました。** 範囲側を変えると、オブジェクト側はそれに `Ctrl` を足したものになります。行き先が既に使われていたら、これまでどおり赤くなって止まります

- 連結のメニューが「右に連結」「下に連結」の 2 つから、**「クリップボードの画像を連結する...」の 1 つ**になりました

- **掴むのに、線の上をぴたりと押す必要はありません。** 少し外れていても掴めます。**カーソルが十字矢印になっていれば掴めます** ―― 見えているとおりに動きます。囲んで選ぶときは帯のぶんを足さず、**囲んだとおりに選ばれます**

- **選んだものの印が、その形に沿うようになりました。** これまでは全体を囲む四角い枠でしたが、斜めの線 1 本でも大きな四角が出てしまい、そのどこでも掴めるように見えていました。いまは**形に沿った白い帯**で示され、**帯の内側がそのまま掴める範囲**です。文字だけは囲みで、傾けていれば囲みも一緒に傾きます

## v0.3.0 — 2026-08-30

### 追加

- **置いたものを選んで、動かせるようになりました。** `Ctrl + W` で矩形、`Ctrl + L` で手描きで囲みます。ペンの線・塗りつぶし・文字が対象です。`Shift` で足し、`Alt` で引くのは範囲選択と同じです。**選んだものをドラッグすればまとめて動きます。** 同じキーをもう一度押すと元のツールに戻るので、描いている途中に 1 本だけ直してすぐ戻れます

- **選んだものを回せるようになりました。** `Ctrl + R`、または右クリックメニューの「オブジェクト」→「回転...」です。画像全体を回すときと同じダイアログで、角度を決めている間**実際に回って見えます**。軸は選んだもの全体の中心です。**文字は回した向きのまま描き直される**ので、斜めにしても輪郭がぼやけません

- **重ね順を変えられるようになりました。** `PageUp` / `PageDown` で 1 つずつ、`Ctrl` を添えると最前面・最背面です。複数選んでいるときは互いの順序を保ったまま動きます

- 設定の「操作の割り当て」に **「オブジェクト」** の見出しが増えました

### 変わったこと

- **モザイク・ぼかしは選択の対象に含めていません。** 置いた時点の絵を抱えている作りなので、動かすと中身が合わなくなるためです

### 直したこと

- **取り消し・やり直しのあと、線や塗りつぶしが古い形のまま描かれることがあったのを直しました。** これまで形がその場で書き換わることが無かったため表に出ていませんでした

## v0.2.0 — 2026-08-18

### 追加

- **直線を引いている間に Alt を押すと、角度が一定ずつに揃うようになりました。** 既定は 15 度ずつです。設定の「描く」タブで変えられ、`0` にすると揃えません。Alt は**引き始めてから**押してください（`Shift + Alt` で押し始めると、直線ではなくスポイトになります）
- **直線を引いている間に Shift をもう一度押すと、そこで折れ曲がるようになりました。** ボタンを離さずに続けて引けます。折れ目の前後で太さはつながるので段差になりません。Alt と併用すれば 15 度ずつの折れ線が引けます

- **画像を好きな角度で回せるようになりました。** 右クリックメニューの「画像」→「自由に回転...」です。角度を決めている間、**ウィンドウの絵がそのまま回ります**（拡大率は変わらないので、細かい所まで見たまま確かめられます）。空いた角は黒で埋め、画像が切れないように広げます。任意の角度では画像を作り直すため少しぼやけるので、その旨をダイアログにも書いてあります（90 度・180 度は従来どおり劣化しません）

- **自由に回転して画像が大きくなったとき、どこを動かさないかを選べるようにしました。** 設定の「全般」→「回転で動かさない所」です。既定は**中央**で、四方へ均等に広がります。**左上**にすると従来どおりウィンドウの左上が動きません

- **選択範囲を複数選べるようになりました。** `Shift + ドラッグ` で足し、`Alt + ドラッグ` で引きます。引こうとしている範囲は赤い枠で出ます。離れた 2 か所を同時に選ぶ、真ん中を抜いてドーナツにする、といったことができます

- **投げ縄で手描きの範囲を選べるようになりました。** 既定は `L` です。離した時点で自動的に閉じます。**範囲選択（`W`）と同じ範囲を共有する**ので、矩形で大きく取ってから投げ縄で細かく足す、という使い方ができます

- **選択範囲を塗りつぶせるようになりました。** 右クリックメニューの「選択範囲」に「塗りつぶし」（`G`）と「マーカー塗りつぶし」（`Shift + G`）が増えています。色・なめらかさはペンの設定をそのまま使います。**消しゴムで消せます**

- **選択範囲の縁に線を引けるようになりました。** 「境界線を描く」（`O`）と「マーカーで境界線を描く」（`Shift + O`）です。色・太さ・なめらかさはペンの設定を使います

- **モザイク・ぼかしにキーが付きました。** `M` と `Shift + M` です

- **線を引いている最中に `R` を押すと、矢印の頭が付くようになりました。** 向きはそのとき線が進んでいた向きです。1 本に何個でも置けます。**頭の大きさはその場所の線の太さで決まる**ので、押す直前に `[` `]` で太さを変えれば頭だけ変えられます。形（幅・長さ・角の丸み）は設定の「描く」で変えられます

- **引き終えた直後でも `R` が効きます。** マウスを動かす以外のことをするまでの間です。引いてから「やっぱり矢印にしたい」と思ったときに引き直さずに済みます

- **`Ctrl + ↑` `Ctrl + ↓` で、置いた直後の矢印の向きを微調整できます。** 刻みは既定 5 度で設定から変えられます。傾けた分は、直線を狙い直しても保たれます

- **変更できないキーも、設定の一覧に載るようになりました。** `[` `]`・`1`〜`5`・`Shift + 1`〜`8`・`Space`・矢印キーです。灰色の行で、選んでも変更ボタンは押せません。何に使われているかを確かめる手段が説明書しか無かったのを改めたものです

- **選択範囲を変えた操作を `Ctrl+Z` で戻せるようになりました。** 引き直し・足し引き・解除・ツールを切り替えて消えた分が、1 つずつ戻せます。**戻すとそのときのツールにも切り替わります**（選択枠は範囲選択・投げ縄でしか描かれないため）

### 変更

- **モザイク・ぼかしが、選んだ形どおりにかかるようになりました。** 複数の範囲でも、穴を開けた形でも、投げ縄の形でも、外側には一切かかりません。以前は外接する矩形にかかっていました

- **離れた場所をまとめて塗ったとき、場所ごとに消せるようになりました。** `Ctrl+Z` は 1 回で全部戻ります。重なっている場所どうしは 1 つとして扱います（分けるとマーカーで重なりだけ濃くなるため）

- **範囲選択と投げ縄では、`Alt + ドラッグ` でスポイトが起動しなくなりました。** `Alt` は「引く」に使うためです。他のツールでは従来どおりで、これらのツールでも `Shift + I` でスポイトは使えます

- **モザイク・ぼかしの初回の強さを、覆っている面積から決めるようになりました。** 斜めの細い帯を選んだときに粗くなりすぎるのを避けるためです。正方形に近い範囲では従来と同じです

- **`Alt + ドラッグ` が `Shift + I` と同じスポイトになりました。** 押している間だけスポイトになり、拡大窓が出て、画面のどこからでも拾えます。離すと元のツールへ戻ります。以前は拡大窓が出ず、拾えるのはウィンドウの上だけでした
- **スポイトで、押したまま動かして色を選び直せるようになりました。** `Shift + I` も同じです。以前は押した瞬間に決まっていました
- **スポイトを使っても選択範囲が消えなくなりました。** 範囲を選んでから色を拾う、という使い方ができます。効果の強さを `[` `]` で調整している最中でも、調整の対象が外れません

### 修正

- **まとめて閉じると、画像がいくつも保存されずに消えていたのを修正しました。** タスクバーから「すべてのウィンドウを閉じる」のように複数のウィンドウを同時に閉じると、自動保存が同じ名前を取り合って、**4 枚閉じても 1 枚しか残らず、その 1 枚も壊れていました**。同じ瞬間に閉じても、それぞれ別の名前で保存されます
- **サインアウト・シャットダウンのときに自動保存されなかったのを修正しました。** 出しっぱなしのウィンドウも、消える前に保存されます
- **Shift を押しながらタスクバーからまとめて閉じても、保存されるようになりました。** Shift で自動保存を飛ばせるのは、**そのウィンドウを前面にして閉じるとき**だけです。タスクバーのメニューは Shift を押しながら開くことがあり、そこで効いてしまうと画面上の全部が黙って消えるためです
- **自動保存に失敗したときに、黙って画像が消えていたのを改めました。** 保存できなかったことを知らせ、**ウィンドウを閉じません**。「開く」「貼り付け」で置き換えるときも、保存できなければ置き換えを中止します
- **範囲を引いている最中に他のアプリへ切り替えると、戻ってきた後もボタンを押していないのに範囲が伸び続けていたのを修正しました。** 引きかけの範囲は破棄され、`Ctrl+Z` で元に戻せます
- **画像の外にはみ出す範囲にモザイク・ぼかしをかけると、ブロックが引き伸ばされていたのを修正しました。** 画像内の他の場所と同じ大きさになります

## v0.1.0 — 2026-08-04

### 追加

- **マウス操作の割り当てを変更できるようにしました。** 画像のスクロール・ウィンドウの移動・拡大縮小・不透明度の 4 つが対象です
- **キーとマウスの割り当てを「操作の割り当て」タブにまとめました。** 編集 / ファイル / ツール / 描く / 表示 / ウィンドウ に分かれています
- **一定時間ウィンドウを隠す機能を追加しました。** 既定は `X` で、隠れている時間は設定で変えられます
- **拡大縮小で動かさない所を選べるようにしました。** 左上 / カーソルの位置 / 画面の中央 の 3 通りで、既定は左上です

### 変更

- **選択した矩形が、モザイク・ぼかしをかけた後も残るようになりました。** 新しく引き直すか、範囲選択以外のツールに切り替えるまで維持されます。モザイクをぼかしに変えたいときに引き直さずに済みます
- **割り当てが重複したときの扱いを変えました。** 以前は元のコマンドから黙って外れていましたが、外さずにその行を赤く表示し、直すまで OK を押せないようにしました
- 設定画面のタブ「ショートカット」を「操作の割り当て」に改めました

### 修正

- **モザイク・ぼかしが、下に描いた線や文字を消していたのを修正しました。** 一緒に隠れるようになります。隠れる中身はかけた時点の見た目で決まります
- 設定画面の説明文が折り返したとき、最後の行が切れることがあったのを修正しました
- 線や文字が多い状態での描画を軽くしました

## 2026-08-03

初版。配布時はバージョン番号を付けていませんでした（`CapturaClipA2_20260803.zip`）。

範囲選択とウィンドウ単位のキャプチャ、ペン / 蛍光マーカー / 消しゴム（筆圧対応）、あとから編集できるテキスト、モザイク・ぼかし、トリミング / 回転 / 反転 / クリップボード画像の連結、キーの割り当て変更、自動保存。
