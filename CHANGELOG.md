# 変更履歴

## 未リリース

### 追加

- **直線を引いている間に Alt を押すと、角度が一定ずつに揃うようになりました。** 既定は 15 度ずつです。設定の「描く」タブで変えられ、`0` にすると揃えません。Alt は**引き始めてから**押してください（`Shift + Alt` で押し始めると、直線ではなくスポイトになります）
- **直線を引いている間に Shift をもう一度押すと、そこで折れ曲がるようになりました。** ボタンを離さずに続けて引けます。折れ目の前後で太さはつながるので段差になりません。Alt と併用すれば 15 度ずつの折れ線が引けます

- **画像を好きな角度で回せるようになりました。** 右クリックメニューの「画像」→「自由に回転...」です。角度を決めている間、**ウィンドウの絵がそのまま回ります**（拡大率は変わらないので、細かい所まで見たまま確かめられます）。空いた角は黒で埋め、画像が切れないように広げます。任意の角度では画像を作り直すため少しぼやけるので、その旨をダイアログにも書いてあります（90 度・180 度は従来どおり劣化しません）

### 変更

- **`Alt + ドラッグ` が `Shift + I` と同じスポイトになりました。** 押している間だけスポイトになり、拡大窓が出て、画面のどこからでも拾えます。離すと元のツールへ戻ります。以前は拡大窓が出ず、拾えるのはウィンドウの上だけでした
- **スポイトで、押したまま動かして色を選び直せるようになりました。** `Shift + I` も同じです。以前は押した瞬間に決まっていました
- **スポイトを使っても選択範囲が消えなくなりました。** 範囲を選んでから色を拾う、という使い方ができます。効果の強さを `[` `]` で調整している最中でも、調整の対象が外れません

## v0.1.0 — 2026-08-04

### 追加

- **マウス操作の割り当てを変更できるようにしました。** 画像のスクロール・ウィンドウの移動・拡大縮小・不透明度の 4 つが対象です
- **キーとマウスの割り当てを「操作の割り当て」タブにまとめました。** 編集 / ファイル / ツール / 描く / 表示 / ウィンドウ に分かれています
- **一定時間ウィンドウを隠す機能を追加しました。** 既定は `X` で、隠れている時間は設定で変えられます
- **拡大縮小で動かさない所を選べるようにしました。** 左上 / カーソルの位置 / 画面の中央 の 3 通りで、既定は左上です

### 変更

- **選択した矩形が、モザイク・ぼかしをかけた後も残るようになりました。** 新しく引き直すか、範囲選択以外のツールに切り替えるまで維持されます。モザイクをぼかしに変えたいときに引き直さずに済みます
- **割り当てが重複したときの扱いを変えました。** 以前は元のコマンドから黙って外れていましたが、外さずにその行を赤く表示し、直すまで OK を押せないようにしました
- 設定画面のタブ「ショートカット」を「操作の割り当て」に改めました

### 修正

- **モザイク・ぼかしが、下に描いた線や文字を消していたのを修正しました。** 一緒に隠れるようになります。隠れる中身はかけた時点の見た目で決まります
- 設定画面の説明文が折り返したとき、最後の行が切れることがあったのを修正しました
- 線や文字が多い状態での描画を軽くしました

## 2026-08-03

初版。配布時はバージョン番号を付けていませんでした（`CapturaClipA2_20260803.zip`）。

範囲選択とウィンドウ単位のキャプチャ、ペン / 蛍光マーカー / 消しゴム（筆圧対応）、あとから編集できるテキスト、モザイク・ぼかし、トリミング / 回転 / 反転 / クリップボード画像の連結、キーの割り当て変更、自動保存。
